# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# 인증정보 — 절대 커밋 금지.
# extract/.env 에 KAMIS·datago(기상청·특일정보와 공용)·MAFRA 키가 들어있다.
.env
*.env
!.env.example

# 편집기·OS
.DS_Store
.idea/
.vscode/

# 노트북 체크포인트
.ipynb_checkpoints/

# 노트북 로컬 캐시 — mart_item_daily 는 뷰라서 조회할 때마다 datago_shipment
# 284MB 를 다시 스캔한다. 셀을 돌릴 때마다 쿼리하지 않도록 parquet 로 받아둔다.
notebooks/.cache/

# scripts/daily.sh 의 cron 실행 로그. 실패 원인을 여기서 찾는다.
logs/
